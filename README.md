# ekwak-temp

이불공장 납품·정산 관리 사이트. 상세 요구사항은 `설계.md`, 작업 목록은
`backlog.json`(CLI: `node tools/backlog.mjs`) 참고.

## 개발 서버 실행

```bash
npm install
npm run dev
```

http://localhost:3000 에서 확인한다.

- `npm run build` — 프로덕션 빌드
- `npm run lint` — ESLint 검사
