# T-002 · Next.js 프로젝트 초기화

> 작업 상태의 SSOT는 저장소 루트의 `backlog.json`이다. 이 문서는 무엇을 왜 어떻게 만드는지만 다룬다.

| 항목 | 값 |
| --- | --- |
| 단계 | 0. 기반 구축 |
| 분류 | 인프라·배포 |
| 우선순위 | P0 |
| 예상 시간 | 30분 |
| 선행 작업 | 없음 |
| 요구사항 근거 | §1 프로젝트 목적 |
| 주요 위치 | `app/, package.json, tsconfig.json` |

## 목적

이후 모든 화면 작업이 올라갈 뼈대를 만든다. 50대 후반 사용자를 위한 큰 글씨·큰 버튼을 기본값으로 잡을 수 있도록 Tailwind 설정까지 이 단계에서 끝낸다.

## 작업 범위

- create-next-app으로 App Router + TypeScript 프로젝트 생성
- Tailwind CSS(v4, CSS-first 설정) 설치 및 기본 글씨 크기 상향
- app/layout.tsx에 한국어 로케일·메타데이터 설정
- ESLint(flat config) 설정과 npm 스크립트 정리 — Prettier는 채택하지 않음
- README에 개발 서버 실행법 기록
- package.json scripts에 `"backlog": "node tools/backlog.mjs"` 등록 (백로그 CLI는 이미 있으나 package.json이 없어 아직 `npm run backlog`로 부를 수 없다)

## 완료 기준

- [x] `npm run dev`로 개발 서버가 뜨고 첫 화면이 렌더된다
- [x] `npm run lint`가 통과한다
- [x] 기본 본문 글씨가 일반 웹사이트보다 크게(최소 18px) 설정됐다

## 주의사항

- 프로젝트 언어는 한국어 고정이므로 `<html lang="ko">`로 둔다.

## 연관 작업

- [T-016](T-016.md) — RLS·서버 권한 경계 구현
- [T-094](T-094.md) — 배포·환경변수·Cron 설정

---

## 쉬운 설명

웹사이트의 뼈대를 만드는 단계다. 앞으로 만들 모든 화면(송장 작성, 정산서, 설정 등)이 이 뼈대 위에 쌓인다. 이 작업에서는 특히 50대 후반 사장님과 직원이 편하게 쓸 수 있도록 글씨와 버튼을 기본값으로 크게 설정한다. 부품을 하나하나 만들기 전에 기본기를 다지는 일이라고 보면 된다.

## 관련 파일

### 이미 있는 파일

| 파일 | 왜 관련 있나 |
| --- | --- |
| `README.md` | 개발 서버 실행법을 기록할 파일 |

### 이 작업에서 만들 파일

| 파일 | 무엇 |
| --- | --- |
| `package.json` | Node.js 의존성과 npm 스크립트 정의 |
| `tsconfig.json` | TypeScript 컴파일 설정 |
| `next.config.ts` | Next.js 설정 파일 (`agentRules: false`로 AGENTS.md 자동 주입 차단) |
| `eslint.config.mjs` | ESLint flat config (Tailwind v4는 `tailwind.config.ts` 없이 CSS에서 설정) |
| `app/layout.tsx` | 루트 레이아웃 (한국어 메타데이터 포함) |
| `app/page.tsx` | 홈 페이지 |
| `app/globals.css` | 전역 스타일 정의 (기본 글씨 18px, 다크 모드 미사용) |

### 먼저 읽을 문서

- `설계.md` §1 — 프로젝트 목적과 사용자 제약
