# T-004 · 전체 DB 스키마 설계 확정

> 작업 상태의 SSOT는 저장소 루트의 `backlog.json`이다. 이 문서는 무엇을 왜 어떻게 만드는지만 다룬다.

| 항목 | 값 |
| --- | --- |
| 단계 | 0. 기반 구축 |
| 분류 | 데이터 모델 |
| 우선순위 | P0 |
| 예상 시간 | 30분 |
| 선행 작업 | [T-003](T-003.md) Supabase 프로젝트 연결 |
| 요구사항 근거 | §3~§24 전반 |
| 주요 위치 | `docs/db-schema.md` |

## 목적

송장·단가·정산이 서로 얽혀 있어 테이블을 하나씩 즉흥적으로 만들면 반드시 뒤집힌다. 품목명 변경 이력 보존, 이월 처리, 예외 단가 같은 까다로운 요구를 미리 스키마에 반영한다.

## 요구사항 근거 (설계.md 인용)

> 품목 이름을 변경하거나 합쳐도 기존 송장에는 당시 사용한 이름을 그대로 보존한다.
>
> 다음 정보는 완전히 삭제하지 않는다.

## 작업 범위

- 엔티티 목록과 관계도(ERD) 작성
- 송장 줄에 '작성 당시 품목명 문자열'을 스냅샷으로 남기는 설계 확정
- 상태 컬럼 값 정의(송장·정산서·단가·발송)
- 삭제 대신 쓰는 상태 컬럼(사용중지/숨김/취소) 정의
- 인덱스 후보 정리(원청별 품목 검색, 월별 송장 조회)

## 완료 기준

- [ ] docs/db-schema.md에 ERD와 테이블별 컬럼 정의가 적혔다
- [ ] 설계.md의 24개 기능 섹션이 모두 어느 테이블로 커버되는지 매핑됐다
- [ ] T-005~T-011 마이그레이션이 이 문서만 보고 작성 가능하다

## 주의사항

- 금액은 부동소수점 대신 정수(원 단위)로 저장한다.
- 송장 줄은 품목 id와 당시 품목명 문자열을 둘 다 갖는다.

## 연관 작업

- [T-005](T-005.md) — 마이그레이션: 공장 정보·직원 계정
- [T-032](T-032.md) — 품목명 변경(이력 보존)
- [T-033](T-033.md) — 여러 품목 합치기
- [T-065](T-065.md) — 뒤늦은 송장 이월 판정 로직

---

## 쉬운 설명

DB 테이블의 뼈대를 그려내는 일이다. 송장, 품목, 단가, 정산이 서로 얽혀 있어 미리 다 생각하지 않으면 나중에 고치기 어렵다. 특히 품목 이름이 바뀌어도 과거 송장에는 옛 이름이 남아야 하고, 동시에 검색과 정산 엑셀에서는 새 이름으로만 보여줘야 하는 까다로운 요구를 스키마에 반영해야 한다. 이 설계 문서가 없으면 T-005부터 T-012까지의 마이그레이션 작업들이 무엇을 만들어야 할지 알 수 없다.

## 관련 파일

### 이미 있는 파일

| 파일 | 왜 관련 있나 |
| --- | --- |
| `supabase/migrations/20260916041354_initial.sql` | T-004에서 만든 스키마를 바탕으로 마이그레이션이 작성됨 |
| `docs/tasks/T-005.md` ~ `T-011.md` | 각 마이그레이션 작업이 T-004의 스키마 설계에 의존 |
| `.claude/rules/도메인-불변식.md` | 금액 계산, 송장번호, 품목명 변경, 이월 규칙 확인 필수 |

### 이 작업에서 만들 파일

| 파일 | 무엇 |
| --- | --- |
| `docs/db-schema.md` | ERD, 테이블별 컬럼, 상태값, 인덱스 전체 정의 |

### 먼저 읽을 문서

- `설계.md` §3~§24 — 원청, 품목, 송장, 단가, 정산, 이월, 권한, 삭제 금지 규칙 전반
- `.claude/rules/도메인-불변식.md` § 송장번호, 품목명 변경, 단가, 정산 계산, 이월, 권한, 삭제 금지 — 스키마에 반영할 제약 조건
