# T-033 · 여러 품목 합치기

> 작업 상태의 SSOT는 저장소 루트의 `backlog.json`이다. 이 문서는 무엇을 왜 어떻게 만드는지만 다룬다.

| 항목 | 값 |
| --- | --- |
| 단계 | 1. 마스터 데이터 |
| 분류 | 기능 구현 |
| 우선순위 | P1 |
| 예상 시간 | 30분 |
| 선행 작업 | [T-032](T-032.md) 품목명 변경(이력 보존) |
| 요구사항 근거 | §4 품목 관리, §4 품목명 변경 규칙 |
| 주요 위치 | `app/owner/items/merge/` |

## 목적

같은 제품이 조금씩 다른 이름으로 여러 개 등록된 상황을 정리한다. 합친 뒤에는 정산서에 최종 이름 하나만 나와야 한다.

## 요구사항 근거 (설계.md 인용)

> 여러 품목을 하나의 최종 품목으로 합치기
>
> 이전 이름은 원청에 보내는 엑셀에 표시하지 않는다.

## 작업 범위

- 합칠 품목 다중 선택과 최종 품목 지정
- merged_into 설정과 합치기 체인 해석
- 합치기 전 영향 미리보기(송장 수·정산 영향)
- 되돌리기 불가 경고와 확인 절차

## 완료 기준

- [ ] 합친 후 정산서에 최종 품목명 하나로 표시된다
- [ ] 과거 송장에는 각자의 당시 이름이 남는다
- [ ] 합치기 체인이 2단이어도 최종 품목이 정확히 해석된다

## 주의사항

- 단가는 품목별로 따로 있으므로 합친 뒤 단가 충돌 처리 규칙을 T-056에서 함께 확인한다.

## 연관 작업

- [T-028](T-028.md) — 품목 검색(이전 이름 포함)
- [T-056](T-056.md) — 송장 줄 ↔ 단가 자동 매칭
- [T-072](T-072.md) — 엑셀 최종 품목명 병합 표시

---

## 쉬운 설명

같은 제품이 조금씩 다른 이름으로 여러 개 등록되는 상황을 정리하는 작업이다. 예를 들어 "퀸 차렵이불 면"과 "퀸 면 차렵"이 사실 같은 제품인데 이름만 다르게 등록돼 있을 수 있다. 합치기를 진행하면 정산서는 최종 이름 하나만 표시되지만, 과거에 쓴 송장에는 각각 당시 이름이 그대로 남아 기록으로 보존된다. 합치기는 되돌릴 수 없으므로 미리보기 화면에서 영향받을 송장 건수나 정산 내역을 확인한 후 경고 문구를 보고 진행해야 한다.

## 관련 파일

### 이미 있는 파일

| 파일 | 왜 관련 있나 |
| --- | --- |
| `supabase/migrations/20260916053523_items_item_aliases.sql` | `merged_into_item_id` 컬럼과 합치기 체인을 따라가는 `final_item_id()`, `item_ids_in_merge_group()` SQL 함수 정의 |
| `lib/items/search.ts` | 활성 품목만 검색하고 합치기 체인 끝까지 따라가 최종 품목 id 반환하는 `searchItems()` 함수 |
| `app/owner/items/page.tsx` | 품목 관리 메인 화면 (품목 목록 표시, 숨김/즐겨찾기 토글) |
| `app/owner/items/actions.ts` | 품목 추가, 즐겨찾기/숨김 토글 액션 (추가 시 `merged_into_item_id is null` 조건 확인) |
| `app/owner/items/rename/page.tsx` | 품목명 변경 화면 — 이 작업에서 합치기 인터페이스의 참고 |
| `app/owner/items/rename/actions.ts` | 품목명 변경 액션 — 이전 이름을 `item_aliases`에 기록하는 패턴 참고 |
| `.claude/rules/도메인-불변식.md` | 품목명 변경·합치기 규칙: 과거 송장은 당시 이름 보존, 정산서는 최종 이름만, 합치기 체인은 2단 이상 가능 |

### 이 작업에서 만들 파일

| 파일 | 무엇 |
| --- | --- |
| `app/owner/items/merge/page.tsx` | 합칠 품목 다중 선택, 최종 품목 지정, 영향 미리보기(송장 수·정산 영향), 경고 및 확인 절차 UI |
| `app/owner/items/merge/actions.ts` | 실제 합치기 트랜잭션 (선택된 품목들의 `merged_into_item_id` 업데이트) |

### 먼저 읽을 문서

- `설계.md` §4 — 품목명 변경 규칙: 과거 송장은 당시 이름 유지, 검색은 이전 이름 포함, 정산서는 최종 이름만
- `.claude/rules/도메인-불변식.md` 품목명 변경·합치기 섹션 (라인 27-38) — 합치기 체인이 2단 이상일 수 있고 최종 품목까지 따라가야 함
