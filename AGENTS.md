# AGENTS.md — 이불공장 납품·정산 관리 사이트

이불공장이 원청에 납품할 때 **송장을 작성·출력**하고, 월말에 납품 내역과
월별 봉제 단가를 결합해 **정산서를 만들어 이메일로 보내는** 웹사이트.

주 사용자는 **50대 후반 공장 사장님과 직원**이다. 컴퓨터·태블릿·휴대폰에서
모두 쓰고, 글씨와 버튼은 크게, 메뉴와 전문 용어는 최소로 한다. 이 제약은
취향이 아니라 요구사항이다 — 화면을 만들 때마다 확인한다.

- 요구사항 원본: `설계.md` (26개 절, 이 문서의 모든 도메인 규칙의 출처)
- 작업 목록: `backlog.json` (96개 작업, 30분 단위) — **CLI로만 다룬다**
- 작업별 상세: `docs/tasks/T-001.md` ~ `T-096.md`
- 스택: Next.js(App Router) + Supabase(Postgres/Auth/Storage) + Vercel

---

## 골든 룰

1. **추측하지 않는다.** 스키마·파일 구조·설정·실행 결과처럼 변할 수 있는 것은
   반드시 조회한 뒤 행동한다. 검증하지 않은 것을 완료로 보고하지 않는다.
2. **`backlog.json`은 직접 열지도 고치지도 않는다.** 조회·수정·추가 모두
   `node tools/backlog.mjs`를 쓴다. (hook이 직접 접근을 차단한다)
3. **작업을 시작하면 반드시 그 작업을 `in_progress`로 바꾼다.**
   `node tools/backlog.mjs set <id> 진행` — 코드를 한 줄이라도 쓰기 **전**이다.
   상태를 안 바꾸고 작업하면 그 작업은 백로그상 아무도 안 하고 있는 것이 된다.
   (hook이 강제한다 — 착수한 작업이 없으면 산출물 파일 수정이 막힌다)
4. **`main`에서 작업하지 않는다.** `dev`에서 작업한다.
5. **돈이 걸린 계산은 요구사항 문구 그대로 구현한다.** 부가세·단가·이월은
   "그게 그거"처럼 보여도 결과가 달라진다. → `.Codex/rules/도메인-불변식.md`
6. **삭제하지 않는다.** 원청·품목·직원 계정·확정 송장·발송된 정산서·월별
   단가는 `숨김`·`사용 중지`·`취소` 상태로만 다룬다.

---

## 작업 흐름

```
1. node tools/backlog.mjs next           지금 시작할 수 있는 작업 (선행 완료된 것만)
2. task-briefer 서브에이전트에 위임       쉬운 설명 + 관련 파일을 docs/tasks/<id>.md에 정리
3. node tools/backlog.mjs set 44 진행      ← 작업 착수 전에 반드시. 건너뛰지 않는다
4. ... 작업 ...
5. adversarial-reviewer 서브에이전트에 위임   파일을 만들거나 고쳤으면 — 치명·중요는 반영
6. node tools/backlog.mjs set 44 done    hook이 전체 커밋 + push
```

**3단계는 어떤 경우에도 빠뜨리지 않는다.** 작업을 하기로 정한 순간 상태를
`in_progress`로 바꾸고 나서 손을 댄다. 작은 작업이라서, 금방 끝나서, 이미
반쯤 했으니까는 이유가 되지 않는다.

2단계와 5단계도 **건너뛰지 않는다.** 정찰 없이 시작하면 이미 있는 파일을 다시
만들고, 리뷰 없이 done으로 넘기면 틀린 금액이 그대로 커밋되고 push된다.
상세는 `.Codex/rules/서브에이전트.md`.

막히면 멈추지 말고 상태로 남기고 다음 작업으로 넘어간다.

| 상황 | 상태 | 예 |
| --- | --- | --- |
| 사용자만 할 수 있는 일 | `needs_decision` | 공장 정보 실제 값, 엑셀 양식 확보 |
| 선행 작업·외부 대기 | `blocked` | T-001이 끝나야 시작 가능 |
| 만들었고 확인이 필요 | `review` | |
| 안 만들기로 함 | `cancelled` | |

`blocked`·`needs_decision`·`cancelled`는 `--note "사유"`가 필수다. 사유 없는
대기 항목은 아무도 풀 수 없다.

턴을 마칠 때 `needs_decision` 항목과 **사용자가 해줘야 할 일**을 함께 보고한다.

---

## 코드 작성

- 한 파일이 길어지면 hook이 막는다. 한도는 `.Codex/code-limits.json`
  (화면 200줄 / API 150줄 / lib 250줄 / 테스트 400줄). 85%에서 경고가 온다.
  **경고가 오면 그때 나눈다.** 한도를 넘긴 뒤 한꺼번에 쪼개는 것보다 싸다.
- 금액은 정수(원)로 다룬다. 부동소수점을 쓰지 않는다.
- 직원에게 금액을 감출 때는 **화면에서 숨기지 말고 응답에서 뺀다.**
  네트워크 탭에 단가가 실려 오면 실패다.
- 코드가 바뀌어 문서가 어긋나면 같은 커밋에서 문서도 고친다.
- 파일을 만들거나 고쳤으면 `done`으로 넘기기 전에 `adversarial-reviewer`에게
  반박을 받는다. lint·build는 문법을 보지만 **부가세 계산이 틀린 것은 못 잡는다.**

자세한 규칙 → `.Codex/rules/코드-작성.md`

---

## 자동으로 강제되는 것 (hook)

무언가 막히면 고장이 아니라 규칙이다. 이유가 메시지에 함께 나온다.

| 언제 | 무엇 |
| --- | --- |
| 세션 시작 | 현재 브랜치 표시, `main`이면 `dev`로 옮기라고 안내 |
| `backlog.json` 접근 | 차단 + CLI 사용법 안내 |
| 산출물 파일 수정 | `in_progress`인 작업이 없으면 **차단** |
| 파일 저장 | 줄 수 한도 검사(85% 경고 / 초과 재작업), 해당 파일 lint |
| Bash 실행 후 | 백로그가 바뀌었으면 자동 커밋 |
| 작업이 `done` | 작업 트리 전체 커밋 + 현재 브랜치로 push |
| 턴 종료 | 코드 변경이 있었으면 build |

lint·build는 `package.json`이 생기는 T-002 이후부터 실제로 돈다.

---

## 규칙 문서

작업 성격에 맞는 것을 **작업 시작 전에** 읽는다.

| 문서 | 언제 읽나 |
| --- | --- |
| `.Codex/rules/도메인-불변식.md` | 송장·단가·정산·권한을 건드리는 모든 작업 **(필수)** |
| `.Codex/rules/백로그-운영.md` | 백로그 상태를 다룰 때, CLI 전체 사용법 |
| `.Codex/rules/코드-작성.md` | 코드를 쓸 때 — 줄 수·배치·UI 제약 |
| `.Codex/rules/서브에이전트.md` | 작업을 시작할 때, 리뷰를 받을 때 **(필수)** |
| `.Codex/rules/git-과-하네스.md` | 브랜치·커밋·hook 동작이 궁금할 때 |
