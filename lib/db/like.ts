/**
 * ilike 패턴에서 `%`·`_`는 와일드카드로 해석된다 — 검색창에 그 문자를
 * 그대로 친 사용자 입력은 리터럴 문자로 취급해야 한다. 검색을 쓰는 모든
 * 화면(원청·품목·단가)이 이 함수 하나를 공유한다 — 화면마다 따로 만들면
 * 한쪽만 고쳐져서 같은 입력에 화면마다 다른 결과가 나온다.
 */
export function escapeLike(raw: string): string {
  return raw.replace(/[%_]/g, (c) => `\\${c}`);
}
